# Windows 硬件信息采集与设备指纹工具 — 开发计划

> 版本：v0.2　日期：2026-09-16　状态：待评审
> 变更：v0.2 采集框架改为**模块化多通道设计**——采集手段可同时注册、并发采集、结果按策略归并（§3.2）
>
> 假设场景：以**软件授权/设备绑定**为主场景（指纹需稳定、可离线校验），同时兼容
> **IT 资产管理**场景（需完整清单导出）。两者对指纹策略要求不同，见 §3.3 与 §6。

---

## 1. 项目概述

### 1.1 目标

开发一个 Windows 平台的硬件信息采集工具：

1. **采集**：枚举本机关键硬件与系统标识信息（CPU、主板、BIOS、内存、磁盘、网卡、显卡、显示器、TPM、系统标识等）；
2. **生成指纹**：将稳定字段加权组合后计算哈希，产出设备指纹（整机指纹 + 部件子指纹）；
3. **输出**：以 JSON 输出完整硬件清单与指纹，供授权校验、资产管理等上层系统使用。

### 1.2 交付物

| 交付物 | 说明 |
|---|---|
| `hwfp.exe` | 单文件 CLI 工具，无第三方运行时依赖 |
| `hwfp.dll`（可选） | 导出 C 接口，供宿主程序集成（授权 SDK 内嵌场景） |
| 硬件清单 JSON Schema | 采集结果的规范化定义文档 |
| 指纹算法说明文档 | 字段清单、权重、哈希流程，供服务端实现一致的比对逻辑 |
| 测试报告 | 覆盖物理机 / 虚拟机 / 硬件变更 / 权限受限等场景 |

### 1.3 非目标（第一期不做）

- 内核态驱动采集（用户态 API 已能满足绝大多数字段）；
- 防篡改/反调试加固（列入二期，见 §9）；
- 跨平台（macOS/Linux）；
- 网络上报与服务端（本期只做本地生成与导出）。

---

## 2. 需求分析

### 2.1 采集项与数据源矩阵（核心需求）

| 类别 | 关键字段 | 数据源 | 稳定性 | 备注 |
|---|---|---|---|---|
| CPU | 厂商、型号、核心/线程数、ProcessorId | `Win32_Processor` + `CPUID` 指令 | 高 | 部分机型 ProcessorId 为 0，需兜底 |
| 主板 | 制造商、型号、序列号 | SMBIOS Type2（`Win32_BaseBoard`） | 高 | 组装板序列号可能为空 |
| 系统/整机 | 制造商、机型、整机序列号 | SMBIOS Type1（`Win32_ComputerSystemProduct`） | 高 | 品牌机/笔记本最可靠字段 |
| BIOS | 厂商、版本、发布日期、序列号 | SMBIOS Type0/1（`Win32_BIOS`） | 中高 | 刷 BIOS 会变 |
| 内存 | 条数、单条容量、厂商、序列号 | `Win32_PhysicalMemory`（SPD） | 中高 | 部分内存序列号为 0 |
| 磁盘 | 型号、序列号、固件版本 | `Win32_DiskDrive`；NVMe 走 `StorageDeviceProperty` | 高 | 只取**系统盘/物理盘**，排除 U 盘；注意序列号字节序（SATA 反转问题） |
| 网卡 | 物理网卡 MAC、厂商 | `GetAdaptersAddresses` 过滤物理网卡 | 中 | MAC 可被软件修改；Win11 Wi-Fi 随机 MAC 需排除；虚拟网卡全部排除 |
| 显卡 | 型号、显存 | `Win32_VideoController` | 中高 | 双显卡取全部列表 |
| 显示器 | EDID 中的厂商+序列号 | 注册表 `DISPLAY\...\Device Parameters\EDID` | 中 | 外接显示器增减会变 → 低权重或不入整机指纹 |
| OS 标识 | `MachineGuid`、`ProductId`、安装日期 | 注册表 `HKLM\SOFTWARE\Microsoft\Cryptography` 等 | 中 | **重装系统会变**，只可低权重参与 |
| TPM | EK/公共部分哈希 | `Tbsi_Context_*` / NCrypt | 高 | Win10/11 普遍启用；VM 中可能没有 |
| 安全启动 | SecureBoot 状态 | `Win32_UEFI` / `Confirm-SecureBootUEFI` 等价 API | — | **只采集不入指纹**（用户可切换） |

> 指纹入选原则：**"跟随硬件、不随软件/用户行为变化"** 的字段才可高权重参与；
> MAC、主机名、IP、盘符、时间类字段禁止直接入指纹。

### 2.2 功能需求

- F1 按类别采集上述字段，单字段失败不中断整体（降级采集，记录失败原因）；
- F2 生成整机指纹（SHA-256）与部件子指纹（cpu/board/disk/os 至少四类）；
- F3 输出 JSON（完整清单+指纹）与简要文本（人工查看）；
- F4 支持配置文件调整：采集通道启停与优先级、字段归并策略、指纹字段开关、权重、输出路径；
- F5 支持静默运行（无 UI、可被授权程序静默调用），运行耗时目标 < 2s（典型物理机）；
- F6 提供校验模式：输入历史清单 JSON，输出当前指纹与历史指纹的**相似度评分**；
- F7 **模块化采集通道**：所有采集手段实现统一接口并自注册进通道注册中心，支持同时注册/启用多种手段并发采集；同一字段多通道结果按策略归并；新增通道不修改核心代码。

### 2.3 非功能需求

| 项 | 要求 |
|---|---|
| 兼容性 | Windows 10 1809+ / Windows 11 / Server 2019+，x64（ARM64 列入二期） |
| 权限 | 普通用户可运行（核心字段均可获取）；管理员权限仅增强个别字段 |
| 依赖 | 零第三方运行库（静态链接 CRT，哈希用 CNG，JSON 库头文件引入） |
| 幂等性 | 同一机器短时间内多次运行，指纹 100% 一致 |
| 隐私合规 | 采集项可配置裁剪；文档中声明清单用途，符合个人信息保护要求（硬件序列号属于设备标识信息） |

---

## 3. 总体设计

### 3.1 技术选型（草案 + 实际落地）

> 下表是立项时的**评估草案**；「实际采用」一列是最终落地结果。本仓库**未引入任何第三方库代码**
> （无 vendored 源码、无第三方链接）：JSON 解析/序列化、单测框架、线程池均为自研。

| 维度 | 候选（评估过） | 理由 | 实际采用 |
|---|---|---|---|
| 语言 | C++17 / C#(.NET 8) / Rust | 单文件无依赖、启动快、便于后续加固与内核扩展 | **C++17** |
| 构建 | CMake / MSVC 工程直接管理 | 工程化、易接 CI | **MSVC 直接构建**（本机无 CMake；`build.cmd`） |
| JSON | nlohmann/json（header-only）/ 手写轻量实现 | JSON 处理省时 vs 彻底零依赖 | **自研 `core/json.*`**（未采用 nlohmann/json） |
| 哈希 | Windows CNG（`BCryptHashData`）/ 纯软件 SHA-256 | 系统自带，无需 OpenSSL | **Windows CNG**（未使用 OpenSSL 或任何密码学库代码） |
| 采集通道（模块） | WMI(COM)、SMBIOS(`GetSystemFirmwareTable`)、注册表、IP Helper、CNG/TPB、CPUID 指令 | 每种手段一个通道模块，统一接口、自注册、并发执行 | 同名五类通道已落地；二期预留 DLL 外置插件通道 |
| 测试 | GoogleTest / Catch2 / 自研 | 单测 + 真机脚本 | **自研零依赖测试框架**（`tests/test_util.h`，57 用例；未引入 GoogleTest/Catch2） |

### 3.2 模块化采集框架（多通道注册、并发采集、结果归并）

**设计原则：每一种采集"手段"都是可插拔的通道模块。** WMI、SMBIOS 直读、注册表、
Win32 原生 API、CPUID 指令……全部实现同一接口，启动时**自注册**进通道注册中心，
按配置**同时启用、并发执行**；同一字段允许多个通道供数，由归并器按策略产出唯一值。

**双维度模型**：

- **通道（Channel）**= 一种采集手段，一个独立模块；
- **字段（FieldKey）**= 一个采集目标（如 `board.serial`），全部集中在 `core/fields.def` 用 X 宏定义（含默认归并策略、可服务通道）；
- 通道与字段**多对多**：一个通道能供多个字段，一个字段可由多个通道供数。

**通道接口（全部手段实现同一接口，新增手段 = 新增一个源文件）**：

```cpp
struct IChannel {
    const char* Name();                       // "wmi" / "smbios" / "registry" / "native" / "cpuid"
    int  DefaultPriority();                   // 数值越小越优先，可被配置覆盖
    bool Available();                         // 运行时探测：WMI 服务是否正常、SMBIOS 表是否存在
    std::vector<FieldKey> SupportedFields();  // 本通道声明可采集的字段
    FieldResult Collect(FieldKey key);        // 单字段采集，失败记 status 不抛异常
};

// 各通道源文件尾一行完成自注册，核心代码零改动：
REGISTER_CHANNEL(WmiChannel)
REGISTER_CHANNEL(SmbiosChannel)
REGISTER_CHANNEL(RegistryChannel)
REGISTER_CHANNEL(NativeChannel)   // GetAdaptersAddresses / CPUID / TPM 等系统 API
```

**运行时流程**：

```
启动 → ChannelRegistry 汇总全部已注册通道
     → 按配置过滤：enable/disable、优先级覆盖
     → Available() 探测，剔除本机不可用通道
     → 按 FieldKey 建任务图，提交线程池并发执行（单任务超时兜底）
     → 每字段收到 N 份候选结果 → 归并器按策略产出唯一值
     → FieldResult{ key, value, source=命中通道, candidates=全部通道原始结果 }
```

**字段归并策略（按字段可配置）**：

| 策略 | 逻辑 | 适用 |
|---|---|---|
| FIRST_BY_PRIORITY | 取优先级最高且非空的结果（降级链） | 默认策略 |
| CONSENSUS | 多通道结果一致 → confidence=high；不一致 → 取高优先级者，分歧记入 candidates | `board.serial`、`disk.serial` 等关键字段（交叉验证，防单通道数据异常） |
| MERGE_LIST | 合并所有通道结果并去重（多重重集语义：保留实例数） | memory / gpu / nic 等列表型字段 |
| CONSENSUS_ALIGNED | 按 `instance_key`（盘位号）把各通道同一实例配对后逐实例交叉验证，输出按盘位数值序 | disk.model/serial/firmware/size_bytes（WMI 返回序非升序，见矩阵 R5） |

**配置示例（config/hwfp.json，与指纹权重同一配置文件）**：

```json
{
  "channels": {
    "smbios":   { "enabled": true, "priority": 10 },
    "native":   { "enabled": true, "priority": 15 },
    "wmi":      { "enabled": true, "priority": 20 },
    "registry": { "enabled": true, "priority": 30 }
  },
  "fields": {
    "board.serial": { "strategy": "consensus" },
    "memory.*":     { "strategy": "merge_list" }
  },
  "execution": { "max_workers": 4, "per_task_timeout_ms": 3000 }
}
```

**新增一种采集手段只需三步**（模块化的验收标准）：

1. 新建 `src/channels/xxx_channel.cpp`，实现 `IChannel`；
2. 文件尾写 `REGISTER_CHANNEL(XxxChannel)`；
3. 默认配置加入条目（可选，缺省按 `DefaultPriority` 自动参与）。

**扩展预留**：`ChannelRegistry` 预留 `LoadChannel(dll_path)` 入口；二期如需外置插件
通道（免重编译主程序），通道 DLL 导出 `CreateChannel()` 即可，接口不变。

**其他要点**：

- 通道实现只依赖 `IChannel` 接口，单测注入 mock 通道，不真跑 WMI/注册表；
- `candidates` 保留所有通道原始结果，既是 CONSENSUS 交叉验证的依据，也是现场排障的诊断信息；
- 输出 JSON 固定 schema 版本号（`schema_version: 1`），服务端按版本解析。

### 3.3 指纹算法设计（关键）

**规范化**：所有入选字段 trim、大小写统一、全半角统一后拼接；
**拼接格式**：`key=value` 用 `\n` 连接，避免字段间歧义；
**整机指纹**：`SHA-256( "HWFP-V1\n" + 各字段行 )`，前 16 字节 hex 即对外指纹。

**权重与子指纹（应对硬件变更）**：

```
整机指纹（严格模式） = 高权重字段集合的哈希
子指纹：fp_cpu / fp_board / fp_bios / fp_disk / fp_os
相似度评分 = Σ(匹配子指纹权重) / Σ(全部子指纹权重)
```

- 服务端校验策略建议：严格相等（授权强绑定）或 相似度 ≥ 阈值（如 0.7，容忍换内存/加硬盘）；
- 字段权重初始建议：整机序列号 30、主板 20、CPU 20、系统盘 20、BIOS 10、内存 10、OS 标识 5、网卡 5、显示器 0（只采集）。
  权重放在**配置文件**，后续实测调整，不改代码。

---

## 4. 里程碑与排期（按 1 名开发全职估算，共约 8 周）

| 阶段 | 内容 | 产出 | 工期 |
|---|---|---|---|
| **M1 技术验证 (POC)** | 逐一验证 2.1 矩阵中每个数据源在目标系统上的可达性与字段质量；跑通 CNG 哈希、WMI COM 初始化 | 采集项可用性矩阵表（真实机器数据）、POC 代码 | 1 周 |
| **M2 采集框架** | IChannel 接口、ChannelRegistry 自注册、线程池并发调度（超时+降级）、归并器（FIRST_BY_PRIORITY / CONSENSUS / MERGE_LIST）、首批通道（WMI/SMBIOS/Registry/Native/CPUID）覆盖 10 类字段 | 库代码 + 单测（mock 通道注入、归并策略用例、并发一致性） | 2 周 |
| **M3 指纹引擎** | 规范化、加权、SHA-256、子指纹、指纹权重配置加载 | FingerprintEngine + 单测（哈希一致性用例） | 1 周 |
| **M4 CLI 与输出** | 参数解析、通道配置加载（启停/优先级/策略）、JSON/文本输出、Schema v1 定稿、校验模式（相似度评分） | `hwfp.exe` 可用版本 | 1 周 |
| **M5 测试与加固** | 见 §5 测试计划；修缺陷；性能优化 | 测试报告、缺陷清单关闭 | 2 周 |
| **M6 发布** | 代码签名、文档（算法说明、集成指南）、打包；可选 DLL 封装 | 发布包 v1.0 | 1 周 |

> 里程碑出口条件：每个 M 结束有可运行产物；M3 结束起指纹算法冻结，只允许改配置不允许改算法。

---

## 5. 测试计划

1. **正确性交叉验证**：与 `msinfo32`、`wmic`（或 `Get-CimInstance`）、CPU-Z、设备管理器逐字段比对（≥5 台不同品牌物理机）；
2. **一致性**：同一机器连续运行 20 次 / 重启后 / 拔插 USB 与非系统盘后，指纹必须一致；
3. **虚拟机**：VMware Workstation、Hyper-V、VirtualBox 各验证一遍（字段缺失的降级路径）；
4. **权限场景**：普通用户、管理员、域环境、组策略禁用 WMI 的模拟；
5. **变更容忍**：加内存条、换网卡、接外接显示器后，相似度评分符合权重预期；
6. **异常注入**：mock Provider 返回空/超时/乱码，验证降级与日志；
7. **性能**：典型机器 < 2s，最差（WMI 慢）场景有超时兜底（单 Collector 3s）。

---

## 6. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| WMI 服务被禁用/损坏 | 大面积字段缺失 | SMBIOS 直读（`GetSystemFirmwareTable`）作为主通道，WMI 反作补充；此为 M1 必须验证项 |
| 序列号为空/全 0（组装机、部分内存） | 唯一性下降 | 字段级有效性校验，空值字段不参与哈希；相似度模式兜底 |
| MAC 可改、Wi-Fi 随机化 | 指纹漂移 | MAC 低权重且只取有线物理网卡； Win11 随机 MAC 识别排除 |
| 重装系统 MachineGuid 变化 | 指纹漂移 | OS 标识仅低权重；文档明确"系统重装视为换绑"策略由上层决定 |
| SATA 序列号字节序/空格差异 | 哈希不稳定 | M1 阶段记录原始值，规范化规则在 M3 冻结并写入算法文档 |
| 杀软误报（采集行为敏感） | 无法分发 | 代码签名；避免使用常被滥用的 API 组合；必要时申请厂商白名单。**注**：gpu.uuid 采集需 spawn `nvidia-smi`（R17）——已用 System32 权威路径、3s 硬超时、句柄白名单收敛，仍属进程创建行为，发布前须实测杀软误报情况 |
| 合规风险 | 法律风险 | 采集清单透明可裁剪，发布《采集说明》；如面向个人用户需取得同意 |

---

## 7. 仓库结构（规划）

```
dimenData/
├─ docs/                 # 本计划、算法说明、Schema 文档
├─ src/
│  ├─ core/              # IChannel、ChannelRegistry(自注册)、调度器、归并器、fields.def
│  ├─ channels/          # 各采集通道模块：wmi / smbios / registry / native / cpuid
│  ├─ fingerprint/       # 规范化、加权、哈希
│  ├─ cli/               # main.cpp、参数解析
│  └─ dll/               # 可选导出接口
├─ tests/                # 单元测试 + 集成脚本(PowerShell)
├─ config/               # 默认指纹配置(hwfp.toml/json)
└─ tools/                # 交叉验证辅助脚本
```

---

## 8. 立即启动项（本周可做）

1. 确认主场景：**授权强绑定**还是**资产管理**（影响相似度策略与加固优先级）；
2. 准备测试环境：≥3 台不同品牌物理机 + VMware 虚拟机（现有 vm-driver-debug-loop 环境可复用）；
3. 执行 M1 POC：按通道拆分写采集 demo（smbios / wmi / registry / native 各一小段），把 2.1 矩阵逐项跑通并记录真实值。

## 9. 二期方向（预留）

- 防篡改加固（反调试、完整性自校验、指纹结果加密签名）；
- 离线授权文件生成/校验闭环（含 RSA 签名）；
- Windows 服务模式定时采集与上报；ARM64 支持；
- 内核态补充采集（如需要抗用户态篡改的字段）。

## 10. 进展记录

- **2026-09-18　T12 网卡逐实例对齐（ConsensusAligned + MAC 键）**：nic.mac/nic.name 从 MergeList 切换为 ConsensusAligned，instance_key=MAC（wmi key_prop=MACAddress、native 连续 hex），merger 新增 `NormalizeInstanceKey`（去 `:- 空格.` + 大写）吸收两通道 MAC 形态差异后逐网卡配对：跨通道一致 confidence=high，名称分歧取高优先级（fallback），无键候选独立成组排在有键组后；ConsensusAligned 输出 note 文案改为中性的"实例对齐/键"（原"盘位"字样对网卡语义不当）。**指纹 A/B 实测逐字节一致**（master 3273C1B6…，验收代理独立复核，无需升 kAlgoVersion——nic.mac 值集合不变、nic.name 权重 0）。内存**暂不做**实例对齐：跨通道槽位键口径（DeviceLocator/BankLocator 取舍）与 PartNumber 等值编码差异未做实测收敛，强行对齐会改变值多重集导致指纹漂移——列为后续任务，落地时需 A/B 实测并评估 kAlgoVersion 升版（此前"本机 SMBIOS locator 为空"的表述系引用 POC 时代 Type17 偏移勘误前的过时观察，原始表复核 DeviceLocator 非空且与 WMI 一致）。**连带修复 D1 遗留敏感泄漏**：fuzz pipeline 语料含本机真实 MAC（从 POC 输出复制的失误，已随 D1 提交与远端快照外泄）→ 替换合成值；全仓 13 类真实标识复扫 0 命中（含本条目——MAC 字面值不再写入任何文件）；publish-clean 快照历史经孤儿分支重写为单笔干净快照强推（远端 main 历史不再含该 MAC）。69 用例全绿。

- **2026-09-18　T12 网卡逐实例对齐（ConsensusAligned + MAC 键）**：nic.mac/nic.name 从 MergeList 切换为 ConsensusAligned，instance_key=MAC（wmi key_prop=MACAddress、native 连续 hex），merger 新增 `NormalizeInstanceKey`（去 `:- 空格.` + 大写）吸收两通道 MAC 形态差异后逐网卡配对：跨通道一致 confidence=high，名称分歧取高优先级（fallback），无键候选独立成组排在有键组后。**指纹 A/B 实测逐字节一致**（master 3273C1B6…，无需升 kAlgoVersion——nic.mac 值集合不变、nic.name 权重 0）。内存**不做**实例对齐（实测 SMBIOS Type17 槽位 locator 为空而 WMI 为 DIMMA2，跨通道无法稳定配对；MergeList 值去重已是正确语义，强行对齐会改变值多重集导致指纹漂移）。69 用例全绿。

- **2026-09-18　B2 hwfp.dll C ABI 集成接口（src/dll/）**：导出 `Hwfp_Version` / `Hwfp_Collect`（默认配置采集→schema v1 JSON）/ `Hwfp_CollectWithConfig`（调用方给配置文本；DLL 不隐式读配置文件）/ `Hwfp_CheckLicense`（签名+有效期+绑定校验，绑定需本机采集，验签先行）/ `Hwfp_Free`。返回码沿用 CLI 退出码语义（0/2/3/5/6）；字符串 UTF-8；静态 CRT，输出缓冲必须 Hwfp_Free 释放。采集入口抽为 `core/collect_runner`（CLI 与 DLL 共用）；版本号收口 `core/version.h`；授权校验输出 JSON 收口 `lic::BuildCheckJson`（CLI/DLL 同形态）。`build.cmd dll`（/LD）/ `dlltest`（构建 `tools/dll_consumer.cpp` 集成消费者并运行，**总是先重建 DLL 防陈旧产物**）。ABI 契约：出参入口即归零（消费者用哨兵指针检出早退路径未清空的缺陷）。dlltest 11 项断言全绿。

- **2026-09-18　B1 离线授权闭环（src/license/）**：算法选型 RSA-2048 + SHA-256 + PKCS#1 v1.5（纯 CNG；Ed25519 不在 CNG 提供范围）。`keygen` 生成密钥对（CNG blob base64 落盘）；`license --issue` 对 canonical 文本（`HWFP-LICENSE-V1` 头 + 固定键序 7 行：licensee/issued_at/expires_at/fingerprint/sub/features/nonce）签名签发，默认采集本机指纹绑定，`--floating` 浮动；`license --check` 验签 + 有效期 + 机器绑定（组权重相似度阈值默认 0.85，与 --verify 口径一致），验签先行快速失败（签名无效不做昂贵采集）。退出码新增 5（格式/签名无效）、6（过期/机器不匹配）。canonical 注入防线：值域禁换行与 `;=`（签发/解析双侧）。fuzz 增 license 目标（被接受授权字段必须与原件一致）。68 用例全绿；E2E：签发→本机校验 valid/sim 1.0/exit 0，篡改→exit 5，过期→exit 6，浮动→exit 0。

- **2026-09-18　D1 GitHub Actions CI + 确定性模糊测试**：`.github/workflows/ci.yml`（windows-latest，`shell: cmd` + vswhere 找 MSVC；步骤=单测→构建→CLI 自检→冒烟采集→fuzz，每周 fuzz-nightly 200 万轮；冒烟输出重定向、公开日志仅摘要行，真实硬件标识不进日志）。`tools/fuzz_main.cpp` 确定性 fuzz（当时六目标，现七目标含 license），oracle 内建（合法基线精确还原、失败必有错误信息、JSON 数值文本自稳定、CSV 独立实现对账、归并不变量、报告往返指纹一致），失败落盘 repro。fuzz 首轮捕获 `NumberToString` %.6g 指数形态文本漂移（修 %.17g）；顺带加固 json 非有限数值拒绝、config 数值域防护。修 build.cmd 两个批处理坑（括号块内 `%errorlevel%` 解析期展开吞退出码；多行脚本调 .cmd 需 `call`）。CI 首跑 success。60 用例全绿；子代理验收 FAIL→修 7 项→复审→PASS。

- **2026-09-17　gpu.uuid 提权入指纹 + 指纹算法版本戳**：`gpu.uuid` 从只采集改为参与指纹（新增 `FpGroup::Gpu` 独立子指纹组，权重 5/165；换卡相似度约扣 3%）。因提权必然改变指纹，引入 **`kAlgoVersion` 算法版本戳**（报告 `fingerprint.algo`，缺省视为 v1）：`--verify` 遇版本不一致输出 `algorithm_changed:true` + `integrity:null` + 退出码 4，与"报告被篡改"（exit 3）区分；同时输出 `weights_override` 提示重算所用权重口径。覆盖局限：仅 NVIDIA 有值，AMD/Intel 平台不产生该组、指纹不变（VM 实测逐字节一致）。57 用例全绿；两轮子代理验收（首轮 PASS+3 P1/4 P2 → 聚焦复审 7/7 关闭）。

- **2026-09-17　gpu.uuid 采集**：实测确认消费级显卡无序列号（R17：WMI/PnP/注册表/nvidia-smi serial 四路径全部不可用），改采 NVIDIA GPU UUID。新增 `gpu.uuid`（权重 0 只采集）、`ParseNvidiaSmiUuidCsv`/`IsNvidiaGpuUuid` 纯函数、`RunCaptureToFile`（临时文件承接 + 3s 硬超时 + `PROC_THREAD_ATTRIBUTE_HANDLE_LIST` 句柄白名单 + System32/`SHGetKnownFolderPath` 权威路径）。真机 2 卡 UUID 入库；VM 降级 note 正确；指纹经基线重建逐字节不变（宿主 3C1F9204…/VM D917F8BB…）。56 用例全绿；验收子代理 PASS（P1 文档 + 4 P2 加固全部处理）。

- **2026-09-17　R5/R6 剩余项收尾**：GPU 虚拟显示适配器过滤上线（PCI 总线白名单 + 名称黑名单；真机证据 GameViewer=`ROOT\DISPLAY`、真显卡=`PCI\VEN_*`，过滤后 gpu 列表仅剩 3 张真显卡）；逐盘共识实例对齐落地（新增 `ConsensusAligned` 归并策略 + `instance_key` 实例对齐键：native 用 PhysicalDriveN、WMI 用 Index；磁盘四字段按盘位配对验证+数值序输出，解决 WMI 返回序非升序的错位问题）。指纹稳定性经 R6 基线版 worktree 对照验证：master 完全一致（GPU 权重 0、磁盘值集合与顺序无关）。49 用例全绿，84 实例 259ms。

- **2026-09-17　R4/R6 收尾完成**：R6 wmi 网卡 PNP 级过滤上线（查询带 PNPDeviceID，PCI/USB 总线判物理，ROOT/SWD/ACPI 排除；真机验证 VMnet 全部剔除（净减 4 实例，90→86；中途 WmiSpec 插列 bug 曾误杀 wmi 磁盘/内存 3 实例，已修复并加防回归断言），287ms）；R4 SATA 字节序对照结论固化（宿主机三块盘两通道逐字符一致，无字节序差异，回归测试 `disk_serial_sata_r4_cross_channel` 锁定）。45 用例全绿。
- **2026-09-17　M2/M3/M4 全部完成（T1–T9）**：核心骨架 → 四通道模块化（smbios/registry/native/wmi）→ 调度器+归并器（三策略/R3 黑名单/R1R2 归一）→ 指纹引擎（整机+子指纹+相似度）→ CLI+配置+JSON（schema v1、verify 完整性校验、tpm_probe 选项）→ 集成验证。每任务均经子代理独立验收（多次 FAIL→修复→复审 PASS）。提交链：667eef9 → 3191524 → bc8b1c5 → a13571c → 8961b4b → 45d8fe8 → a82169a → 46fd6f0 → 953c46e →（T9）。
  - **性能达标**：5388ms → **374ms**。根因=MicrosoftTpm 命名空间 ConnectServer 在部分主板固定阻塞 ~5s（本机实测 5016ms，矩阵 R16）；wmi 通道改按类批量查询+属性缓存（31 查询→11 类）、Available 预热、tpm_probe 默认关闭（SetOption 选项机制）。
  - **双环境验证**：宿主机 90 实例 374ms、同机指纹逐字节一致（3B36F31B…C307）；VMware Win10 测试机 42 实例 477ms，guest 侧 SHA256 校验通过。
  - 剩余待办（后续任务）：TPM 管理员权限复测；≥3 台品牌物理机补测矩阵；wmi 虚拟显示适配器名称黑名单过滤（R6 遗留 GPU 部分）；逐盘共识的实例对齐键设计；代码签名与发布包；M5 防篡改加固（二期）。
- **2026-09-16　M1 POC 宿主机 + 虚拟机双环境验证完成**：四通道采集 demo（smbios / wmi / registry / native）编译为单文件 `poc/poc_hwfp.exe`（/MT 零依赖），宿主物理机（Win11 25H2 / 9800X3D）与 VMware Win10 测试机全部跑通（guest 侧 SHA256 校验通过）。沉淀 **15 条规范化规则**（ProcessorId 双字节序、占位符黑名单、WMI 磁盘序列号下划线、幽灵 EDID、WMI PhysicalAdapter 不可靠等），详见 `docs/M1-POC-采集项可用性矩阵.md`。
  - 待办：管理员权限复测 TPM、补 ≥3 台品牌物理机、SATA 序列号字节序对照、vTPM 验证。
  - 性能：宿主机串行 5.2s（超 2s 目标 → M2 并发调度解决），VM 172ms。
  - 工具链备注：编译器为 VS 18 Insiders MSVC 14.51（vswhere 查不到该实例，直接用 vcvars64.bat，见 `poc/build.cmd`）；MSVC 源码需 `/utf-8`；iptypes.h 的 GAA 段要求先包含 winsock2.h。
